[pytest]
# WHY pythonpath: Tells pytest where to find our app modules
# Without this, "from app.config import ..." would fail in tests
pythonpath = .
testpaths = tests
# Show verbose output and print statements
addopts = -v -s